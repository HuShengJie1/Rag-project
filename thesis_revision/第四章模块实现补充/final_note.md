# 第4章模块实现补充说明

## 已新增内容

- 已完成第 4 章结构审查，并整理为 `chapter4_structure_audit.md`。
- 已为 4.1、4.2、4.3、4.4 分别撰写新增的“模块实现”小节，内容汇总于 `added_sections.tex`，并已同步追加到主论文 `thesis/毕业论文.tex` 的对应模块末尾。
- 已整理 9 张代码截图建议，覆盖 MinerU2.5 解析调用、语义切块与元数据绑定、`bge-m3` 模型加载与批量编码、向量数据库写入、初筛检索、重排序、Prompt 构造、引用标记解析、引用到元数据映射。
- 已整理 4 个流程图建议，覆盖四个新增实现小节，可直接据此绘图。

## 对老师要求的响应情况

从文本结构上看，当前补充稿已经能够正面回应“每个模块在选型之后补充自己系统中的实现”这一要求，原因如下：

- 每个模块均新增了“输入输出说明”“系统实现流程说明”“实现重点与工程意义说明”三部分文字。
- 每个模块均预留了 1 个流程图占位 figure。
- 4.1、4.2、4.3 各预留了 2 个代码截图占位 figure，4.4 为便于展示“回答后处理”链路，拆分为 3 个代码截图占位 figure。
- 图题、文件名与截图清单已统一，便于后续替换为正式图片。

因此，就论文补写材料而言，当前内容已经足以支撑将第 4 章补充为“核心模块设计与实现”的完整表述。

## 仍需人工补充的部分

- 仍需人工按照 `flowchart_suggestions.md` 绘制 4 张正式流程图，并替换 `figures/chapter4/` 下的占位文件名。
- 仍需人工根据 `code_screenshot_checklist.md` 截取 9 张正式代码图，并替换 `figures/chapter4/` 下对应图片。
- 若后续需要实际编译论文，需确保上述图片文件已经创建；当前 LaTeX 已使用占位回退写法，缺图时也可保留版面说明，但正式交稿前仍建议替换为真实图片。

## 需要明确说明的实现边界

- 当前项目中的向量数据库实际采用 Chroma，相关实现已在补充稿中按真实代码描述，未采用 Milvus。
- 当前项目中的生成引擎实际通过 Kimi 兼容接口接入，后端代码位于 `api/core.py` 与 `api/api_chat.py`。
- 当前 PDF 入库链路中的页码信息依赖 Markdown 中是否存在页码锚点；`src/chunkers/chunking.py` 已实现 `page_labels` 提取逻辑，但 `src/loaders/mineru_loader.py` 当前按文档级返回 Markdown 文本，因此页码字段的精度仍取决于上游解析结果中是否保留页码标识。该点已在截图建议中如实保留，未作编造性扩展。
